# Copy to .env.development.local (gitignored) so Vite proxies /api to YOUR machine.
# Fixes 404 on Admin → WhatsApp ops when the default proxy target is production and
# production has not deployed /api/admin/whatsapp-ops yet.
#
#   cp .env.development.local.example .env.development.local
#
# Use the same port as GuideXpert-Backend (PORT in backend .env, often 5000).
# Prefer leaving VITE_API_URL unset in dev: the app uses relative /api and this target.
# Do not set VITE_API_URL=http://localhost:5000 unless you really want a direct browser
# connection (then the API must be listening on that port). Loopback VITE_API_URL
# values are treated like empty and still go through /api unless VITE_DIRECT_LOCAL_API=true.
VITE_PROXY_TARGET=http://127.0.0.1:5000
