This folder contains the blog cover image pool used when admins don't upload an image.

- 20 images (SVG) are preloaded and imported by `src/utils/blogImagePool.js`.
- Rotation logic avoids repeating the same image consecutively.
