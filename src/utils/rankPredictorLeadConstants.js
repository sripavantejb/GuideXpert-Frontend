/** UTM tagging for rank-predictor organic leads (admin list filter: utm_content exact match). */
export const RANK_PREDICTOR_LEAD_UTM = {
  utm_source: 'student_tool',
  utm_medium: 'organic',
  utm_campaign: 'rank_predictor',
  utm_content: 'organic_rank_predictor',
};

/** Required occupation string for POST /send-otp when capturing rank-predictor leads. */
export const RANK_PREDICTOR_LEAD_OCCUPATION = 'Student — Rank predictor';
