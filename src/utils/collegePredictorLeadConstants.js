/** UTM tagging for college-predictor organic leads (admin list filter: utm_content exact match). */
export const COLLEGE_PREDICTOR_LEAD_UTM = {
  utm_source: 'student_tool',
  utm_medium: 'organic',
  utm_campaign: 'college_predictor',
  utm_content: 'organic_college_predictor',
};

/** Required occupation string for POST /send-otp when capturing college-predictor leads. */
export const COLLEGE_PREDICTOR_LEAD_OCCUPATION = 'Student — College predictor';
